package com.cryptoexchanges.core.ds.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes

val CryptoShapes = Shapes(
    small = RoundedCornerShape(CryptoDimens.radiusS),
    medium = RoundedCornerShape(CryptoDimens.radiusM),
    large = RoundedCornerShape(CryptoDimens.radiusL),
)
